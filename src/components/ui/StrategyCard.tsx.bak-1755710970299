import React from 'react';

export default function StrategyCard({ title, description }: { title: string; description: string }) {
  return (
    <div className="bg-white p-4 rounded-xl shadow-sm hover:shadow-md transition w-full sm:w-[32%]">
      <h4 className="text-lg font-semibold text-blue-600">{title}</h4>
      <p className="text-sm text-gray-600 mt-2">{description}</p>
    </div>
  );
}
