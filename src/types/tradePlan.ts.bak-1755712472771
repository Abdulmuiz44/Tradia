export interface TradePlan {
  id: string;
  symbol: string;
  setupType: string;
  plannedEntry: number;
  stopLoss: number;
  takeProfit: number;
  riskReward: number;
  lotSize: number;
  reason: string;
  confidence: number;
  preChecklist: string[];
  emotion?: string;
  date: string;
  screenshotUrl?: string;
  status: "Planned" | "Executed" | "Canceled" | "Missed";
}
